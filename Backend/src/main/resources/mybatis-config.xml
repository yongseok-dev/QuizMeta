<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties>
        <!-- MyBatis 설정에 필요한 환경 변수를 설정합니다. -->
    </properties>
    <typeAliases>
        <!-- 클래스 별칭 정보를 설정합니다. -->
    </typeAliases>
    <mappers>
        <mapper resource="mapper/member.xml"/>
    </mappers>
</configuration>
